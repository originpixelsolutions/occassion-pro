// This file is superseded by the events/[id]/ folder.
// Expo Router gives precedence to the folder; this file is kept
// only because the filesystem does not allow deletion from this context.
export { default } from './[id]/index'
